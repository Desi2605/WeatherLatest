<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission for Internet access -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherApp"
        tools:targetApi="31">

        <!-- Activities -->
        <activity android:name=".LandingPageActivity" android:exported="true" android:theme="@style/Theme.WeatherApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="true" android:label="@string/app_name" android:theme="@style/Theme.WeatherApp" />
        <activity android:name=".SignUpActivity" android:exported="true" android:theme="@style/Theme.WeatherApp" />
        <activity android:name=".SignInActivity" android:exported="true" android:theme="@style/Theme.WeatherApp" />
        <activity android:name=".WeatherActivity" android:exported="true" android:theme="@style/Theme.WeatherApp" />
        <activity android:name=".ProfileActivity"/>


    </application>
</manifest>
